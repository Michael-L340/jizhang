> ⚠️ 这是 2026-09-03 立项时的初版草案，多处已被实现推翻（托管从 Vercel 改为 GitHub Pages、账户可不指定、分类分两级等）。
> **现状以 `README.md` 和 `CLAUDE.md` 为准**，本文件仅作历史记录保留。

# 个人记账 PWA — 架构方案 v1（2026-09-03）

## 一、产品定位
只给自己用的「记账 + 个人财务看板」。手机上当 App 用（PWA 添加到主屏幕），电脑上开网页，数据同一份。

## 二、V1 功能清单（只做这 9 个）
1. 记一笔：支出 / 收入 / 转账，三步完成（金额 → 用途 → 保存）
2. 五类支出用途（写死，可在设置里改名）
   - 日常餐饮：正常校园吃饭消费
   - 娱乐消费：娱乐餐饮、游戏充钱
   - 经常生活开支：通勤、水电、房租
   - 非经常生活消费：礼物、衣服
   - 意外开支：其他
3. 收入分类：工资/实习、生活费、奖学金、理财收益、退款、其他
4. 四个固定账户：中国银行、招商银行、支付宝、微信，每笔账必选账户
5. 账户余额：系统按流水自动推算，你随时可以手动输入「实际余额」，差额自动生成一笔「余额校准」记录
6. 流水页：按月查看、筛选（账户 / 用途 / 类型）、编辑、删除
7. 统计页：饼图（本月支出用途占比）、柱状图（近 12 个月收入 vs 支出）、折线图（本月每日累计支出 / 各账户余额走势）
8. 首页看板：本月收入、支出、结余、储蓄率、四账户余额卡片、最近流水
9. 导出：一键导出 CSV（备份用）

V2 再考虑：预算、微信/支付宝账单导入、周期账单、自然语言记账、搜索、标签。

## 三、页面结构
底部 5 个 Tab：首页 ｜ 流水 ｜ ＋记账 ｜ 统计 ｜ 账户

首页
┌────────────────────────────┐
│ 2026 年 9 月               │
│ 收入 ¥x  支出 ¥x  结余 ¥x   │
│ 储蓄率 xx%                 │
├────────────────────────────┤
│ 中国银行 ¥x   招商银行 ¥x   │
│ 支付宝  ¥x    微信    ¥x    │
├────────────────────────────┤
│ 本月支出用途（小饼图+列表） │
├────────────────────────────┤
│ 最近流水                   │
└────────────────────────────┘

记一笔
┌────────────────────────────┐
│   支出   收入   转账       │
│        ¥ 28.00             │
│ 用途（5 个大按钮）         │
│ 账户（4 个按钮，记住上次） │
│ 备注 / 日期（默认今天）    │
│         [ 保存 ]           │
└────────────────────────────┘

账户页
每个账户：推算余额、上次校准时间、「输入实际余额」按钮 → 弹窗填数 → 生成校准记录。

## 四、数据模型（Supabase PostgreSQL，3 张表）

accounts
  id, name, sort, initial_balance, created_at
  （固定 4 行：中国银行、招商银行、支付宝、微信）

categories
  id, name, kind('expense'|'income'), icon, sort, note
  （支出 5 行 + 收入 6 行，预置）

transactions
  id, date, type('expense'|'income'|'transfer'|'adjust'),
  amount, category_id, account_id, to_account_id(转账目标),
  note, created_at, updated_at

账户余额 = initial_balance
         + 该账户所有 income
         − 该账户所有 expense
         + 转入 − 转出
         + adjust（校准差额，可正可负）

关键设计：
- 转账不计入收入/支出统计，只影响两个账户的余额
- 校准（adjust）不计入收入/支出统计，只影响余额，但在流水里可见、可撤销
- 所有表都带 user_id，开 RLS，只有你的账号能读写

## 五、技术选型
前端：Vite + React + TypeScript + Tailwind CSS
图表：ECharts（饼/柱/折线都现成，中文友好，手机上可缩放）
PWA：vite-plugin-pwa（manifest + service worker，一行配置）
后端/数据库：Supabase（PostgreSQL + 登录 + 自动生成 API），前端直接用 supabase-js 读写，不需要自己写服务器
托管：Vercel（首选）或 Cloudflare Pages / GitHub Pages（备选）
代码：GitHub 私有仓库

选静态站而不是 Next.js 的原因：单用户 CRUD 没有服务端逻辑，纯前端 + Supabase 更简单、部署更自由、打包更快。

## 六、部署步骤（全免费，约 30 分钟）
1. 注册账号：GitHub、Supabase（GitHub 登录）、Vercel（GitHub 登录）
2. Supabase 新建项目（区域选 Singapore 或 Tokyo）
   → SQL Editor 跑我给的建表脚本（含 4 账户 + 11 分类预置）
   → Authentication 里新建一个用户（你的邮箱 + 密码），关闭注册入口
3. 本地开发：我在这个目录写代码，`npm run dev` 本地跑，手机同 WiFi 可直接访问测试
4. 推到 GitHub 私有仓库
5. Vercel → Import 该仓库 → 填 2 个环境变量：
   VITE_SUPABASE_URL、VITE_SUPABASE_ANON_KEY → Deploy
   → 得到 https://xxx.vercel.app
6. iPhone Safari 打开 → 分享 → 添加到主屏幕 → 桌面出现图标
7. 电脑浏览器直接开同一个网址

## 七、要提前知道的坑
- 国内访问 *.vercel.app 时好时坏。解决办法：买个域名（¥30-60/年）绑到 Vercel，或换 GitHub Pages / Cloudflare Pages 托管。数据库 supabase.co 一般可正常访问。
- Supabase 免费项目连续 7 天无访问会暂停，登录控制台点一下即可恢复。记账天天用，基本碰不到。
- 数据只有一份在云端，所以统计页放「导出 CSV」按钮，建议每月导一次存到电脑。
- PWA 在 iPhone 上离线只能看缓存的页面，V1 不做离线记账（V2 可加 IndexedDB 本地缓存）。

## 八、成本
Vite/React/ECharts：¥0   Supabase：¥0   Vercel：¥0   GitHub：¥0
可选域名：¥30-60/年
